package com.pixelro.nenoonkiosk.core.constants

import android.util.SizeF

object GlobalValue {
    var pixelDensity = 1f
    var screenWidthDp = 1
    var screenHeightDp = 1
    var focalLength = 1f
    var lensSize = SizeF(1f, 1f)

    var statusBarPadding = 1f
    var navigationBarPadding = 1f

    var isLandscape = true  // 화면 방향 (기본값: 가로모드)
}
